<h1>Welcome, <%= @user.username %></h1>

<% unless admin? %>
   <h2>Your books:</h2>
   <ul><% @user.books.each do |book| %>
      <li><%= link_to "#{book.title}", book_path(book) %></li>
      <li>By, <i><%= link_to book.author.name, author_path(book.author) %></i></li>
      <li><i>Book Quantity: <%= book.downloads.find_by(book_id: book.id).quantity %></i></li>
      <br>
      <%# <%= render '/books/book_li', book: book %>
   </ul><% end %>
   <br>
   <%= link_to "Your reviews", user_reviews_path %>
   <br>
   <br>
   <%= link_to "Delete account", url_for(action: :delete), method: :get %>
<% end %>