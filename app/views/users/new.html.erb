<%= link_to "Home", root_url %>
<%= link_to "Login", login_path %>
<h1>Signup</h1>

<%# render the form errors partial, send it @user as the object %>
<%= render partial: "layouts/form_errors", locals: {object: @user } %>

<%= render 'form' %>