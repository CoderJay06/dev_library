<div class="category-container">
   <h1><%= @category.name %> Books</h1>
   <ul class="category-books-list"><% @category.books.each do |book| %>
      <li class="category-book-link"><%= link_to book.title, book_path(book) %></li>
   <% end %></ul>
</div>